name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # Runs every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP Ping
        run: curl -s https://youtube-automation-engine.onrender.com/ > /dev/null
